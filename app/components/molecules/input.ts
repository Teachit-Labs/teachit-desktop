// TODO: Add variants
// TODO: Should this be a component?
import { tv } from "tailwind-variants";

export const input = tv({
  base: "bg-neutral-50 border border-neutral-200 rounded-sm py-2 px-4",
  variants: {},
  defaultVariants: {},
});
